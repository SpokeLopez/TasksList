doctype html
html
    head
    title Nueva tarea
    body
        form(action="/tasks" method="POST")
            textarea(name="descripcion" placeholder="Descripcion de la tarea")
            input(type="submit" value="Agregar")